/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: "https://roomquestaccomodations.com", // your live domain
  generateRobotsTxt: true, // generates robots.txt
  sitemapSize: 5000, // optional: splits sitemap if many pages
};
